import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Remove ignoreBuildErrors to catch TypeScript issues
  // Remove ignoreDuringBuilds to catch ESLint issues
};

export default nextConfig;
